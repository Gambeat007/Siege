import server.ServerApp;

public class Main {
    public static void main(String[] args) {

        ServerApp server = new ServerApp();

        //start Connection
        server.startRunning();
    }
}
